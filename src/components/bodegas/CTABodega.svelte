<section class="cta-bodega">
  <div class="inner">
    <h2>¿Querés ser bodega socia fundadora?</h2>

    <p>Estamos validando la primera versión del ecosistema Cava24.</p>

    <a href="#contacto-section" class="cta-btn">Postular mi bodega</a>
  </div>
</section>

<style>
  .cta-bodega {
    padding: 4rem 2rem;
    background: var(--cava-burgundy-deep, #6a1f33);
    color: #fff;
    text-align: center;
  }

  .inner h2 {
    font-size: clamp(2rem, 3vw, 2.6rem);
    margin-bottom: 0.8rem;
  }

  .inner p {
    margin-bottom: 2rem;
    opacity: 0.9;
  }

  .cta-btn {
    padding: 0.9rem 1.8rem;
    border-radius: 999px;
    background: var(--cava-yellow, #f2c94c);
    color: #4b0e1c;
    font-weight: 700;
    text-decoration: none;
    display: inline-block;
    box-shadow: 0 20px 40px rgba(0,0,0,0.4);
    transition: transform 0.2s ease, filter 0.2s ease;
  }

  .cta-btn:hover {
    transform: translateY(-2px);
    filter: brightness(1.05);
  }
</style>
